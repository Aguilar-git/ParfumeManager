/*const METHOD = Object.freeze({
    GET: "GET",
    POST: "POST",
    PUT: "PUT",
    DELETE: "DELETE"
});

export default class Fetch {
    static async send(method, url, headers, data) {
        const sendData = data

        return fetch(url, {
            method,
            headers,
            body: JSON.stringify(body),
            body: JSON.stringify(body),
        }).then((result) => result.json());
    }

    static async _get(url, headers, params) {
        return this.send(METHOD.GET, url, headers, params)
    }


    static async _post(url, headers, body) {
        return this.send(METHOD.POST, url, headers, body)
    }
}*/

